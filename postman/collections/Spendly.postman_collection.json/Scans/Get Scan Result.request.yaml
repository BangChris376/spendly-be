$kind: http-request
description: Returns scan details including status (pending | processing | completed | failed), merchant_name, total_amount, scan_date, suggested_category, confidence_score, and confidence_level (high | medium | low).
url: "{{base_url}}/scans/{{scan_id}}"
method: GET
order: 2000
